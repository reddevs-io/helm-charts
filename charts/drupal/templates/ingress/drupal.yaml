{{- if .Values.drupal.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "drupal.drupal.fullname" . }}
  labels:
    {{- include "drupal.drupal.labels" . | nindent 4 }}
  {{- with .Values.drupal.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.drupal.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.drupal.ingress.tls }}
  tls:
    {{- range .Values.drupal.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.drupal.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "drupal.drupal.fullname" $ }}
                port:
                  number: {{ $.Values.drupal.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
