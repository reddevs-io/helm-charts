{{- $viteFullname := include "vite.fullname" . -}}
{{- $viteName := include "vite.name" . -}}
{{- $viteChart := include "vite.chart" . -}}
{{- range .Values.workers }}
{{- /* `autoscaling` is optional per worker — default to an empty dict so an
       omitted block reads as disabled instead of nil-dereferencing. */}}
{{- $workerAutoscaling := .autoscaling | default dict -}}
{{- if and .enabled $workerAutoscaling.enabled }}
{{- $workerFullname := printf "%s-%s" $viteFullname .name -}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ $viteName }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
    app.kubernetes.io/name: {{ $viteName }}
    {{- if $.Chart.AppVersion }}
    app.kubernetes.io/version: {{ $.Chart.AppVersion | quote }}
    {{- end }}
    component: "worker-{{ .name }}"
    helm.sh/chart: {{ $viteChart }}
    tier: worker
  name: {{ $workerFullname }}
spec:
  maxReplicas: {{ $workerAutoscaling.maxReplicas }}
  metrics:
    {{- if $workerAutoscaling.targetCPUUtilizationPercentage }}
    - resource:
        name: cpu
        target:
          averageUtilization: {{ $workerAutoscaling.targetCPUUtilizationPercentage }}
          type: Utilization
      type: Resource
    {{- end }}
    {{- if $workerAutoscaling.targetMemoryUtilizationPercentage }}
    - resource:
        name: memory
        target:
          averageUtilization: {{ $workerAutoscaling.targetMemoryUtilizationPercentage }}
          type: Utilization
      type: Resource
    {{- end }}
  minReplicas: {{ $workerAutoscaling.minReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $workerFullname }}
{{- end }}
{{- end }}