{{- if and .Values.redis.enable .Values.redis.env.secret }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    {{- include "outline.redis.labels" . | nindent 4 }}
  name: {{ include "outline.redis.fullname" . }}
stringData:
  {{- range $key, $val := .Values.postgres.env.secret }}
  {{ $key }}: {{ $val | quote }}
  {{- end}}
type: Opaque
{{- end }}
