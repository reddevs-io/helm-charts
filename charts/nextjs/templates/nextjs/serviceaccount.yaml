{{- if .Values.serviceAccount.create -}}
apiVersion: v1
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
kind: ServiceAccount
metadata:
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "nextjs.labels" . | nindent 4 }}
  name: {{ include "nextjs.serviceAccountName" . }}
{{- end }}
