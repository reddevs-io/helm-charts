apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "vite.labels" . | nindent 4 }}
  name: {{ include "vite.fullname" . }}
spec:
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "vite.selectorLabels" . | nindent 4 }}
  type: {{ .Values.service.type }}
