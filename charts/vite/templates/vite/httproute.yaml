{{- if .Values.gatewayApi.enabled -}}
{{- if not .Values.gatewayApi.httpRoute.parentRefs }}
{{- fail "gatewayApi.httpRoute.parentRefs must contain at least one reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  {{- with .Values.gatewayApi.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "vite.labels" . | nindent 4 }}
  name: {{ include "vite.fullname" . }}
spec:
  {{- if .Values.gatewayApi.httpRoute.hostnames }}
  hostnames:
    {{- range .Values.gatewayApi.httpRoute.hostnames }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  parentRefs:
    {{- range .Values.gatewayApi.httpRoute.parentRefs }}
    - name: {{ .name }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
      {{- if .port }}
      port: {{ .port }}
      {{- end }}
      {{- if .sectionName }}
      sectionName: {{ .sectionName }}
      {{- end }}
    {{- end }}
  rules:
    {{- range .Values.gatewayApi.httpRoute.rules }}
    - backendRefs:
        {{- if .backendRefs }}
        {{- range .backendRefs }}
        - name: {{ .name }}
          port: {{ .port }}
          {{- if .weight }}
          weight: {{ .weight }}
          {{- end }}
        {{- end }}
        {{- else }}
        - name: {{ include "vite.fullname" $ }}
          port: {{ $.Values.gatewayApi.httpRoute.backendPort | default $.Values.service.port }}
        {{- end }}
      {{- if .filters }}
      filters:
        {{- toYaml .filters | nindent 8 }}
      {{- end }}
      {{- if .matches }}
      matches:
        {{- range .matches }}
        - {{- if .headers }}
          headers:
            {{- range .headers }}
            - name: {{ .name }}
              type: {{ .type }}
              value: {{ .value }}
            {{- end }}
          {{- end }}
          {{- if .method }}
          method: {{ .method }}
          {{- end }}
          {{- if .path }}
          path:
            type: {{ .path.type | default "PathPrefix" }}
            value: {{ .path.value | default "/" }}
          {{- end }}
          {{- if .queryParams }}
          queryParams:
            {{- range .queryParams }}
            - name: {{ .name }}
              type: {{ .type }}
              value: {{ .value }}
            {{- end }}
          {{- end }}
        {{- end }}
      {{- end }}
      {{- if .timeouts }}
      timeouts:
        {{- toYaml .timeouts | nindent 8 }}
      {{- end }}
    {{- else }}
    - backendRefs:
        - name: {{ include "vite.fullname" $ }}
          port: {{ $.Values.gatewayApi.httpRoute.backendPort | default $.Values.service.port }}
      matches:
        - path:
            type: PathPrefix
            value: /
    {{- end }}
{{- end }}
