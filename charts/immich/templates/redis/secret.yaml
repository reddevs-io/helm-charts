{{- if and .Values.redis.enabled .Values.redis.env.secret }}
apiVersion: v1
kind: Secret
metadata:
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
  labels:
    {{- include "immich.redis.labels" . | nindent 4 }}
  name: {{ include "immich.redis.fullname" . }}
stringData:
  {{- range $key, $val := .Values.redis.env.secret }}
  {{ $key }}: {{ $val | quote }}
  {{- end}}
type: Opaque
{{- end}}