{{- if .Values.storybook.enable -}}
{{- if .Values.storybook.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "nextjs.storybook.fullname" . }}
  labels:
    {{- include "nextjs.storybook.labels" . | nindent 4 }}
  {{- with .Values.storybook.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.storybook.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.storybook.ingress.tls }}
  tls:
    {{- range .Values.storybook.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.storybook.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "nextjs.storybook.fullname" $ }}
                port:
                  number: {{ $.Values.storybook.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- end }}
