{{- if .Values.drupal.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "drupal.drupal.fullname" . }}
  labels:
    {{- include "drupal.drupal.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "drupal.drupal.fullname" . }}
  minReplicas: {{ .Values.drupal.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.drupal.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.drupal.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.drupal.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.drupal.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.drupal.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
