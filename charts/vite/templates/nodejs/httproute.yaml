{{- if and .Values.nodejs.enable .Values.nodejs.gatewayApi.enabled -}}
{{- $nodejsFullname := printf "%s-nodejs" (include "vite.fullname" .) -}}
{{- if not .Values.nodejs.gatewayApi.httpRoute.parentRefs }}
{{- fail "nodejs.gatewayApi.httpRoute.parentRefs must contain at least one reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  {{- with .Values.nodejs.gatewayApi.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "vite.nodejs.labels" . | nindent 4 }}
  name: {{ $nodejsFullname }}
spec:
  {{- if .Values.nodejs.gatewayApi.httpRoute.hostnames }}
  hostnames:
    {{- range .Values.nodejs.gatewayApi.httpRoute.hostnames }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  parentRefs:
    {{- range .Values.nodejs.gatewayApi.httpRoute.parentRefs }}
    - name: {{ .name }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
      {{- if .port }}
      port: {{ .port }}
      {{- end }}
      {{- if .sectionName }}
      sectionName: {{ .sectionName }}
      {{- end }}
    {{- end }}
  rules:
    {{- range .Values.nodejs.gatewayApi.httpRoute.rules }}
    - backendRefs:
        {{- if .backendRefs }}
        {{- range .backendRefs }}
        - name: {{ .name }}
          port: {{ .port }}
          {{- if .weight }}
          weight: {{ .weight }}
          {{- end }}
        {{- end }}
        {{- else }}
        - name: {{ $nodejsFullname }}
          port: {{ $.Values.nodejs.gatewayApi.httpRoute.backendPort | default $.Values.nodejs.service.port }}
        {{- end }}
      {{- if .filters }}
      filters:
        {{- toYaml .filters | nindent 8 }}
      {{- end }}
      {{- if .matches }}
      matches:
        {{- range .matches }}
        - {{- if .headers }}
          headers:
            {{- range .headers }}
            - name: {{ .name }}
              type: {{ .type }}
              value: {{ .value }}
            {{- end }}
          {{- end }}
          {{- if .method }}
          method: {{ .method }}
          {{- end }}
          {{- if .path }}
          path:
            type: {{ .path.type | default "PathPrefix" }}
            value: {{ .path.value | default "/" }}
          {{- end }}
          {{- if .queryParams }}
          queryParams:
            {{- range .queryParams }}
            - name: {{ .name }}
              type: {{ .type }}
              value: {{ .value }}
            {{- end }}
          {{- end }}
        {{- end }}
      {{- end }}
      {{- if .timeouts }}
      timeouts:
        {{- toYaml .timeouts | nindent 8 }}
      {{- end }}
    {{- else }}
    - backendRefs:
        - name: {{ $nodejsFullname }}
          port: {{ $.Values.nodejs.gatewayApi.httpRoute.backendPort | default $.Values.nodejs.service.port }}
      matches:
        - path:
            type: PathPrefix
            value: /
    {{- end }}
{{- end }}
