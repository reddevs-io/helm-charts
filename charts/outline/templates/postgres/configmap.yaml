{{- if and .Values.postgres.enable .Values.postgres.env.normal }}
apiVersion: v1
data:
{{- range $key, $val := .Values.postgres.env.normal }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
kind: ConfigMap
metadata:
  labels:
    {{- include "outline.postgres.labels" . | nindent 4 }}
  name: {{ include "outline.postgres.fullname" . }}
{{- end }}
