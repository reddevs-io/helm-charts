{{- if .Values.nextjs.env.secret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nextjs.nextjs.fullname" . }}
type: opaque
stringData:
  {{- range $key, $val := .Values.nextjs.env.secret }}
  {{ $key }}: {{ $val | quote }}
  {{- end}}
{{- end -}}
